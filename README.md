cmd command for starting application:
java -jar dist/coworking-fat-jar.jar

cmd command for starting docker container with application:
docker compose up -d

to rerun
docker-compose down --volumes
docker-compose up --build  